name: Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure deploy dir exists
        run: mkdir -p /home/vazelinc4ik/pineventory

      - name: Sync to deploy dir
        run: |
          rsync -a --delete --exclude '.git' --exclude '.github' ./ /home/vazelinc4ik/pineventory/

      - name: Ensure venv
        run: |
          python3 -V
          python3 -m venv /home/vazelinc4ik/pineventory/app-venv || true
          /home/vazelinc4ik/pineventory/app-venv/bin/python -m pip install -U pip setuptools wheel

      - name: Install deps
        run: |
          /home/vazelinc4ik/pineventory/app-venv/bin/pip install -r /home/vazelinc4ik/pineventory/requirements.txt

      - name: Migrate DB (optional)
        run: |
          cd /home/vazelinc4ik/pineventory
          if [ -f alembic.ini ] || [ -d alembic ]; then
            /home/vazelinc4ik/pineventory/app-venv/bin/alembic upgrade head || true
          fi

      - name: Restart service
        run: sudo systemctl restart pineventory.service
